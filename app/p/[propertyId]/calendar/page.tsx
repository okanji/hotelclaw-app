/**
 * Calendar index landing. The actual grid is rendered by `<CalendarSurface>`
 * in the property layout, so this page is `null` and exists only so the
 * `/calendar` URL resolves.
 */
export default function CalendarPage() {
  return null;
}
