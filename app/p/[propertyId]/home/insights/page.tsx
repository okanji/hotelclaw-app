// The Insights surface is rendered by `<InsightsSurface>` in the property
// layout (it matches on the `/home/insights` URL prefix), so this route's page
// renders nothing — the same null-page pattern every other section uses.
export default function InsightsPage() {
  return null;
}
