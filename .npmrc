# React Native / Expo needs a flat node_modules to resolve native modules and
# their peers reliably. This is the layout the official Expo monorepo guide
# recommends for pnpm (https://docs.expo.dev/guides/monorepos/). Next.js is
# unaffected by a hoisted layout.
node-linker=hoisted
