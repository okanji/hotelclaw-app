// The org chart is rendered by `<OrgSurface>` in the property layout (it
// matches on the `/home/org` URL prefix), so this route's page renders
// nothing — the same null-page pattern every other section uses.
export default function OrgPage() {
  return null;
}
