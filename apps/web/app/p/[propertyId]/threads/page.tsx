/**
 * Threads landing. The view is rendered by `<ThreadsSurface>` in the
 * property layout (it reads the active URL), so this page is `null` and
 * exists only so the `/threads` URL resolves on a hard load / deep link.
 */
export default function ThreadsPage() {
  return null;
}
