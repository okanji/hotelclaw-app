/**
 * Tasks index landing. The board is rendered by `<TasksSurface>` in
 * `tasks/layout.tsx` (when there's no `taskId` in the URL), so this page is
 * `null` and exists only so the `/tasks` URL resolves.
 */
export default function TasksPage() {
  return null;
}
