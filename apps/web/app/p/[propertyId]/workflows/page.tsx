/**
 * Workflows index — rendered by `<WorkflowsSurface>` in the property layout
 * when the URL matches `/p/<pid>/workflows`. This page is `null` so rail
 * navigation can `pushState` between sections without round-trips.
 */
export default function WorkflowsPage() {
  return null;
}
